# Mystikos Admin 开发约定

## 文档同步

每次修改本目录下的项目代码时，必须检查并同步维护 Markdown 文档。

- 功能、交互、字段或模块发生变化时，更新 `README.md` 的已实现功能和交互说明。
- 依赖、命令或环境要求发生变化时，更新技术栈、环境要求和 npm 命令。
- 目录结构发生变化时，更新项目结构。
- API、环境变量或部署方式发生变化时，补充对应配置和使用说明。
- 完成开发计划中的项目时，同步更新复选框状态。
- 每次实质性功能开发都在 `README.md` 的变更记录中增加简短条目。

文档内容必须描述当前真实实现，不应把模拟数据、浏览器内存操作描述为已经接入后端或完成持久化。

## 验证

代码变更完成后至少运行：

```bash
npm run build
```

涉及页面和交互的变更，还应启动开发服务器并在浏览器中检查对应页面与控制台。
